export default interface IButtonProps {
  /**
   * Button contents
   */
  label: string;
  /**
   * isButton Primary? (true for Pink background and false for white background)
   */
  isPrimary?: boolean;
}
